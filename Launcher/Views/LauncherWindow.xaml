<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModels="clr-namespace:FoM.Launcher.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="FoM.Launcher.Views.LauncherWindow"
        Title="Face of Mankind Preinstall" Height="310" Width="600" WindowStartupLocation="CenterScreen"
		FocusManager.FocusedElement="{Binding ElementName=txtUsername}" Loaded="Window_Loaded">
	<Window.Resources>
		<ResourceDictionary>
			
			<ViewModels:LauncherWindowViewModel x:Key="LauncherWindowViewModelDataSource" d:IsDataSource="True"/>
		</ResourceDictionary>
    </Window.Resources>
	<Window.Background>
		<ImageBrush ImageSource="/FoM.Launcher;component/Resources/FOTD.png" Stretch="UniformToFill"/>
	</Window.Background>
	<Window.Triggers>
		<EventTrigger RoutedEvent="FrameworkElement.Loaded"/>
	</Window.Triggers>
	<Grid DataContext="{Binding Source={StaticResource LauncherWindowViewModelDataSource}}">
		<ProgressBar HorizontalAlignment="Right" Height="25" VerticalAlignment="Bottom" Width="200" Margin="0,0,10,10" Value="{Binding PatchProgress, Mode=OneWay}"/>
		<TextBlock HorizontalAlignment="Right" TextWrapping="Wrap" Text="We are preinstalling Face of Mankind" VerticalAlignment="Top" Foreground="#FFC5C5B4" Margin="0,10,10,0"/>
	</Grid>
</Window>
